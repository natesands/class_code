#include <iostream>
#include <cctype>
#include <string>

//PROTOTYPE FUNCTIONS

int main () {

  //get word from user
  //convert word to all caps
  //get word's pronunciation
  //fetch listword
  //check if idenltical
  //check if listword has one extra phoneme than word, and append
  //to list if so
  //check if listword has one less phoneme than word
   
